{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "bob.serviceAccountName" . }}
  labels:
    {{- include "bob.labels" . | nindent 4 }}
{{- end }}
