apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bob.fullname" . }}-mcp-config
  labels:
    {{- include "bob.labels" . | nindent 4 }}
data:
  mcp_servers.json: |
    {{- .Values.bob.mcpServers | nindent 4 }}
